# version: '3.9'

# services:
#  node-app:
#    container_name: my-container
#    image: my-node-app
#    ports:
#      - "4000:4000"
#    volumes:
#      - /Users/amol/Desktop/MyProjects/Docker/node-docker-app:/app
#    working_dir: /app
#    stdin_open: true
#    tty: true
services:
  img:
    build: .
    container_name: my-container
    ports:
      - "4000:4000"
    volumes:
      - .:/app
    working_dir: /app
    stdin_open: true
    tty: true